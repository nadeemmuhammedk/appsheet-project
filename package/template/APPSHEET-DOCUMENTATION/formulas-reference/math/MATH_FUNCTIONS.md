# AppSheet Math Functions

Math functions perform numeric calculations.

## 1. Common Functions
- `SUM(list)`
- `AVERAGE(list)`
- `MAX(list)` / `MIN(list)`
- `ROUND(number)`
- `ABS(number)`

## 2. Examples
- `SUM([Related Orders][Total])`
- `ROUND([Subtotal] * 1.18, 2)`
